<header>
  <div class="row">
    <div class="col-md-2">
      <%= image_tag("k.png", alt: "KeyMan") %>
    </div>
    <div class="col-md-5">
      <span><h1>Cryptographic Keys Manager</h1></span>
    </div>
    <div class="col-md-3">
      <nav class="top_menu">
        <ul>
          <% if user_signed_in? %>
          <li> <%= t('User') %>: <%= link_to (current_user.prenom + ' ' + current_user.nom), edit_user_registration_path %></li>
          <li> | <%= link_to t('Sign_out'), destroy_user_session_path, method: "delete" %></li>
          <% else %>
          <li> <%= link_to t('Sign_in'), new_user_session_path %></li>
          <% end %>
          <li> | <%= link_to t('Help'), "#" %> </li>
        </ul>
      </nav>
    </div>
  </div>
</header>
